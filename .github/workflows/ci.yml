name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npx tsc --noEmit -p site
      - run: npm test
      - run: npm run build
      - run: npm run docs:agents
      - run: git diff --exit-code -- site/public/llms.txt site/public/llms-full.txt AGENTS.md README.md README.ja.md README.zh-CN.md
      - run: npm run site:build
